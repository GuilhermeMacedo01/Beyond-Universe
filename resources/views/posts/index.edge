@layout('layout/main')

@section('home')
<div>
    <span>Lista de Posts:</span>
</div>
<div>
  <ul>
    @each (post in posts)
    <li>
        <a href="{{ route('posts.show',{id:post.id}) }}"> {{post.title}}</a>
           id:({{post.id}})
    </li>
    @end
</ul>
</div>
@end