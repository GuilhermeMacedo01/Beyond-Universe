@layout('layout/main')

@section('home')
@entryPointStyles('post')

<section class="container">
  <div>
    <h1>{{ post.title }}</h1>
    <p>{{ post.user.user_name }}</p>
    <hr>
  </div>

  <div>
    <p>{{ post.content }}</p>
  </div>
</section>

@end